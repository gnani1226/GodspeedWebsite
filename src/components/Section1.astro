<section class="main-section">
    <div class="section1-class gap-4">
     <div>
        <p class="achieve-class">ACHIEVE</p>
        <h1 class="quantum">QUANTUM LEAP </h1>  
            <h1 class="spanclass">IN PRODUCTIVITY</h1>
     </div>
    
    <div>
        <h2 class="develop-h2">Develop, Deploy & Monitor with Godspeed</h2>
        <p class="nextpara">The next-gen application command center that integrates with your development stack.</p>
    </div>
    <div class="buttons-div">        
        <div class="input-div">
            <input type="text" name="email" value="Enter your email to get early access" class="input-class" />
        </div>
        <div class="all-btns">
            <div class="btn-div">
                <button class="btn">GET EARLY ACCESS</button>
            </div>
                <div class="empty-div">hh</div>
                <div class="empty-div">hh</div>
        </div>
    </div>
    </div>
    <!-- <div class="section2-div">
        <h1 class="bg-transparent">Gnaneshwar</h1>
    </div>   -->
         
</section>

<!-- hsl(33, 100%, 50%) -->
<!-- rgba(199,58,0, 1) -->
<style>

    .main-section{
        min-height: 100vh;
        background-color: black;
        background: radial-gradient(circle at bottom right ,#FF8C00,
        orangered,black,black,black,black);
        display: flex;
        justify-content: flex-start;
        align-items: center;
        padding-left: 50px;

        }
    .all-btns{
        width: 460px;
        display: flex;
        justify-content: flex-start;
        align-items: center;

    }

    .empty-div{
        box-shadow: 0px 16px 28px -20px ragb(199,58,0, 1);
        opacity: 0.4;
        background-color: rgba(255,140,0, 1);
        height: 88px;
        color: rgba(255,140,0, 1);
        margin-top: 20px;
        margin-left: 10px;
        /* box-shadow: 16px 0px 28px 10px #C73A00; */
        width: 20px;
        
        
    }

    .spanclass{
        opacity: 1;
        color: #BDD4DE;
        font-family: sans-serif;
        font-size: 40px;
        font-weight: 400;
        font-style: italic;
        letter-spacing: 2px;
        text-align: left;
        display: inline;


    }


    .spanclass {
        display: inline;
        overflow: hidden; /* Ensures the content is not revealed until the animation */
        border-right: .15em solid orange; /* The typwriter cursor */
        white-space: nowrap; /* Keeps the content on a single line */
        margin: 0 auto; /* Gives that scrolling effect as the typing happens */
        letter-spacing: .15em; /* Adjust as needed */
        animation: 
            typing 3.5s steps(30, end),
            blink-caret .5s step-end infinite;
}

/* The typing effect */
@keyframes typing {
  from { width: 0 }
  to { width: 100% }
}

/* The typewriter cursor effect */
@keyframes blink-caret {
  from, to { border-color: transparent }
  50% { border-color: orange }
}

    .section1-class{
        background-color: transparent;
        width: 60%;
        display: flex;
        flex-direction: column;
        justify-content: center;    
        align-items: flex-start;
        padding-left: 50px;
    }

    .section2-div{
        width: 30%;
        background: radial-gradient(circle at bottom right,rgba(199,58,0, 1),rgba(255,140,0, 1))
        

    }

    .achieve-class{ 
    opacity: 1;
    color: rgba(122,122,122,1);
    /* font-family: "DMSans-9ptRegular_Medium"; */
    font-family: sans-serif;
    font-size: 24px;
    font-weight: 500;
    font-style: normal;
    letter-spacing: 2px;
    text-align: left;

    }

    .quantum{
        opacity: 1;
        color: rgba(255,255,255,1);
        /* font-family: "DMSans-9ptRegular_ExtraBold"; */
        font-family: sans-serif;
        font-size: 40px;
        font-weight: 800;
        font-style: normal;
        letter-spacing: 4px;
        text-align: left;
        display: inline;
    }
.develop-h2{
    opacity: 1;
    color: rgba(204,204,204,1);
    /* font-family: "DMSans-9ptRegular_Bold"; */
    font-family: sans-serif;
    font-size: 32px;
    font-weight: 700;
    font-style: normal;
    letter-spacing: 2.28px;
    text-align: left;

}
.nextpara{
    opacity: 1;
    color: rgba(143,143,143,1);
    /* font-family: "DMSans-9ptRegular_Regular"; */
    font-family: sans-serif;
    font-size: 24px;
    font-weight: 400;
    font-style: normal;
    letter-spacing: 0px;
    text-align: left;
    line-height: 32px;

}

.btn{
        opacity: 1;
    color: rgba(0,0,0,1);
    /* font-family: "DMSans-9ptRegular_Black"; */
    font-family: sans-serif;
    font-size: 28px;
    font-weight: 900;
    font-style: normal;
    letter-spacing: 2px;
    text-align: center;
    line-height: 32px;
    }
    .btn-div{
        opacity: 1;
    background-color: rgba(255,140,0, 1);
    margin-top: 20px;
    width:380px;
    height: 88px;
    display: flex;
    justify-content: center;
    align-items: center;
    }

    .input-div{
        border-radius: 2px;
        border: 1px solid rgba(255,255,255, 0.14);
        opacity: 1;
        height: 88px;
        width: 460px;
        display: flex;
        justify-content: center;
        align-items: center;
    }

    .input-class{
        opacity: 1;
        color: rgba(173,173,173,1);
        /* font-family: "DMSans-9ptRegular_Medium"; */
        font-family: sans-serif;
        font-size: 24px;
        font-weight: 500;
        font-style: normal;
        letter-spacing: 0px;
        text-align: left;
        line-height: 32px;
        width: 420px;
        background-color: transparent;
 
    }


</style>